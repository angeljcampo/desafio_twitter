<nav class="navbar navbar-success">
  <a href="<%= root_path %>"><i class="fas fa-swimmer fa-3x text-light"></i></a>

      <div class="my-2 my-lg-0">
        <% if user_signed_in? %> 
        <%= link_to 'Log out',
        destroy_user_session_path,method: :delete, class: "btn btn-light" %> 
        <% else %> 
        <%= link_to 'Admin', new_admin_user_session_path, class: "btn
        btn-outline-danger mr-2"%> <%= link_to 'Log in', new_user_session_path,
        class: "btn btn-light mr-2"%> <%= link_to 'Register now',
        new_user_registration_path, class: "btn btn-light mr-3" %> 
        <% end %>
      </div>
</nav>

